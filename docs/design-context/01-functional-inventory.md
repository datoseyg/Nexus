# 01 — Inventario funcional

Nivel de capacidad (módulo × funcionalidad), no de pantalla — el detalle de composición de UI está en `08-screen-evidence-matrix.md`. Commit base: `4a3d055` (`supabase-migration`), salvo donde se cita otra branch explícitamente.

Leyenda de estado: **AS_IS** (implementado y verificable ahora) · **COMMITTED** (contratado/prometido formalmente, con o sin implementación) · **TO_BE** (propuesta futura no aprobada). Cada fila también lleva el estado de confianza CONFIRMADA/PARCIALMENTE CONFIRMADA/LEGACY/PROPUESTA/DESCONOCIDA.

## A. Pipeline de datos (sin superficie de UI — módulos que ningún actor humano opera desde un navegador)

| Funcionalidad | Estado | Actor | Entrada | Acción | Resultado | Evidencia técnica |
|---|---|---|---|---|---|---|
| Extracción Zendesk (tickets) | AS_IS · CONFIRMADA | Operador vía CLI (`npm run get:zendesk`) | Credenciales `ZENDESK_URL/USER/TOKEN`, endpoint `search.json` | `GET /api/v2/search.json?query=type:ticket...`, paginado, máx. 10 páginas | JSON crudo en `data/raw/zendesk/` | STATIC_CODE `src/miners/zendesk.js:15-45` |
| Backfill Zendesk por ID (291+361+4 IDs puntuales) | AS_IS · CONFIRMADA | Operador vía CLI (`npm run get:zendesk:backfill-fieldbeat`) | Lista de IDs desde `data/reports/fieldbeat_tasks_linked_to_missing_zendesk_ticket.csv` | `GET /tickets/{id}.json` uno a uno, throttled 300ms | JSON en `data/raw/zendesk/backfill_by_fieldbeat_ticket_ids/`; distingue 404 de 403 | STATIC_CODE `src/miners/zendesk-backfill-missing-ticket-ids.js:35-93` |
| Extracción FieldBeat (tasks, producción) | AS_IS · CONFIRMADA | Operador vía CLI (`npm run get:fieldbeat:all`) | `FIELDBEAT_API_BASE_URL/USER/PASS` | Paginación por cursor `next_page`, máx. `FIELDBEAT_MAX_PAGES` (default 1000) | `data/raw/fieldbeat/list_pages/`, `all_tasks_latest.json`, `task_index.json` | STATIC_CODE `src/miners/fieldbeat-all.js:65-161` |
| Extracción FieldBeat puntual por IDs | **LEGACY/roto** · CONFIRMADA (ausencia de función) | — | `FIELDBEAT_TASK_IDS` | Referencia una variable `taskId` nunca definida (bug) | No produce salida utilizable | STATIC_CODE `src/miners/fieldbeat.js:44,50` — no listado en `package.json`, no forma parte del flujo real (`docs/DATA_PIPELINE.md:19`) |
| Extracción Dolibarr (productos) | AS_IS · CONFIRMADA | Operador vía CLI (`npm run get:dolibarr`) | Header `DOLAPIKEY` | `GET /api/index.php/products?limit=100&page=N`, máx. 50 páginas | `data/raw/dolibarr/products_page_*.json` | STATIC_CODE `src/miners/dolibarr.js:14-45` |
| Normalización RAW→PROCESSED (×3 plataformas) | AS_IS · CONFIRMADA | Operador vía CLI (`npm run normalize:*`) | JSON en `data/raw/<plataforma>/` | Dedup por ID, tipado estable, explode de listas numeradas en FieldBeat (`explodeUsedPartItems`) | CSVs `DB_*`/`DIM_*` en `data/processed/` | STATIC_CODE `src/normalizers/{zendesk,fieldbeat,dolibarr}-normalizer.js` |
| Resolución de identidad de repuestos | AS_IS · CONFIRMADA | Invocado por `build:used-parts-dolibarr-match` | `DB_FieldBeat_Used_Parts.csv` + `DIM_Dolibarr_Product_Identity_Map.csv` + alias manuales | Cascada: alias manual → rechazo de placeholder → REF/BARCODE/ID exacto → normalizado exacto → `REF_LIKE` → `NO_MATCH`; múltiples candidatos → `AMBIGUOUS_MATCH` | Clasificación `match_status` por fila | STATIC_CODE `src/resolvers/part-identity-resolver.js:151-216` |
| Construcción de marts (4 builders) | AS_IS · CONFIRMADA | CLI (`npm run build:*`) | CSVs PROCESSED + resolución de identidad | Joins Zendesk↔FieldBeat↔Dolibarr | `data/marts/*.csv` (5 archivos) | STATIC_CODE `src/marts/build-{used-parts-dolibarr-match,ticket-fieldbeat-view,ticket-fieldbeat-dolibarr-view,fieldbeat-report-dolibarr-view}.js` |
| QA de ingestión (duplicados/cobertura, ×3 plataformas + multiplicidad ticket↔task) | AS_IS · CONFIRMADA | CLI (`npm run qa:*`) | CSVs PROCESSED/MARTS | Chequeos de integridad referencial y duplicados | JSON en `data/reports/*_audit_report.json` — **no escribe a ninguna tabla SQL** | STATIC_CODE `src/qa/audit-{zendesk,fieldbeat,dolibarr,ticket-fieldbeat-multiplicity}.js`; AUSENCIA CONFIRMADA de escritura a `audit.*` · comando `grep -rn "audit\.\|pipeline_runs\|data_quality_events" src/qa/` · commit `4a3d055` |
| Reconciliación de alcance (gate previo a GOLD) | AS_IS · CONFIRMADA | CLI (`npm run qa:scope-reconciliation`) | Mart ticket-Dolibarr | Determina qué tasks FieldBeat están "en alcance" del mart ticket-céntrico | `data/reports/scope_reconciliation_summary.json`, consumido directo por `build-gold.js` | STATIC_CODE `src/qa/reconcile-ticket-fieldbeat-dolibarr-scope.js:24` |
| Construcción GOLD (ticket-céntrico + report-céntrico) | AS_IS · CONFIRMADA | CLI (`npm run build:gold`, `build:gold:fieldbeat`) | Marts + reportes QA | Agregación final con estados de calidad de dato | 11 CSVs en `data/gold/` | STATIC_CODE `src/gold/build-gold.js:272`, `build-fieldbeat-gold.js:209` |
| Warehouse local DuckDB (init/load/validate) | AS_IS · CONFIRMADA | CLI (`npm run db:build`) | 26 CSVs configurados en `warehouse-config.js` | `CREATE OR REPLACE TABLE ... read_csv_auto` + conteo de validación | `data/warehouse/eyg_nexus.duckdb` | STATIC_CODE `src/db/{init,load,validate}-duckdb.js` |
| Migración a Supabase Postgres | AS_IS · CONFIRMADA | CLI (`npm run db:pg:build`) | DuckDB vivo (introspección) | Genera DDL (`sql/010-030`), trunca+recarga Postgres, valida, registra en `audit.warehouse_sync_state` | Postgres poblado; **único punto donde `src/` escribe a un schema transaccional (`audit.warehouse_sync_state`)** | STATIC_CODE `src/db/generate-postgres-ddl.js`, `migrate-to-supabase.js:26-32,60-101,155-160`, `validate-supabase.js:196-202` |

## B. Producto — pantallas activas (detalle de composición en `08`)

| Funcionalidad | Estado | Actor | Entrada | Acción | Resultado | Evidencia técnica |
|---|---|---|---|---|---|---|
| Explorador genérico de tablas | AS_IS · CONFIRMADA (LOCAL_RUNTIME: `GET /api/tables` → 200, 40 tablas) | Usuario anónimo | Selección de schema/tabla, paginación, orden, filtro de una columna | `SELECT` validado contra `information_schema` (allowlist `processed/marts/gold`) | Tabla paginada + export CSV client-side (solo página actual) | STATIC_CODE `app/explorer/page.tsx`, `app/api/tables/**/route.ts`, `lib/sql-guardrails.ts:32-92` |
| Búsqueda libre por palabra clave | AS_IS · CONFIRMADA (LOCAL_RUNTIME: `GET /api/search?q=bomba` → 200, 45 resultados) | Usuario anónimo | Texto libre (hasta 6 keywords ≥2 caracteres) | ILIKE AND/OR sobre `marts.fieldbeat_report_dolibarr_operational_view` + `processed.fieldbeat_report_fields` | Resultados + panel de "SQL ejecutada" visible | STATIC_CODE `app/search/page.tsx`, `app/api/search/route.ts:106-166` |
| Dashboard FieldBeat (report-céntrico) | AS_IS · CONFIRMADA (LOCAL_RUNTIME: 200, kpis+5 series) | Usuario anónimo | Ninguna (sin filtros) | 5 consultas fijas a GOLD | 7 KPIs + 3 rankings | STATIC_CODE `app/dashboard/fieldbeat/page.tsx`, `app/api/dashboard/fieldbeat/route.ts:10-51` |
| Dashboard Operacional (filtrable, cross-filter) | AS_IS · CONFIRMADA (LOCAL_RUNTIME: `/summary` 200, `totalRegistros=3747`, `totalTickets=628`) | Usuario anónimo | 11 filtros (cliente/tipoTarea/máquina/fecha/etc.) | Auto-exclusión de la dimensión propia por gráfico | KPIs, series temporales, matriz máquina×cliente, export JSON client-side | STATIC_CODE `components/dashboard/OperationalDashboardTab.tsx`, `app/api/dashboard/operacional/{filters,summary,parts,detail}/route.ts` |
| Tab Uptime/Downtime | AS_IS · CONFIRMADA, con auto-advertencia explícita de alcance | Usuario anónimo | Filtros de fecha/cliente | Suma `duration_minutes` por tipo de tarea | **No es downtime real** — la propia respuesta trae `downtimeWarning:true` (confirmado LOCAL_RUNTIME) y `hcCalc/uptimePct/tha/hcTeorica` son `null` fijo | STATIC_CODE `app/api/dashboard/uptime/{summary,table,tasks}/route.ts:8-16,84-87,116` |
| Dashboard Trabajo Fuera de Horario | AS_IS · CONFIRMADA (LOCAL_RUNTIME: `businessHoursStatus="MISSING"`, `holidaysStatus="MISSING"`, confianza 79.3 "Media") | Usuario anónimo | Filtros cliente/técnico/tipo/fecha/nivel de confianza | Agregación ponderada por duración sobre `marts.fieldbeat_working_hours_analysis` **en vivo** | 6 KPIs con score de confianza metodológica (nunca estadística, por diseño) | STATIC_CODE `app/api/dashboard/after-hours/summary/route.ts:11-20`; ver `05-business-metrics.md` para el detalle de la fórmula |
| Auditoría y Validación Manual — lectura (6 tabs) | AS_IS · CONFIRMADA (LOCAL_RUNTIME: todas 200) | Usuario anónimo | Filtros cliente/máquina/fecha/texto | Listados filtrados de partes/tickets/reportes con calidad de dato baja | Tablas paginadas + `suggested_action` textual | STATIC_CODE `app/api/audit/{summary,ambiguous-parts,parts-review,placeholders,reports-review,ticket-links-review}/route.ts` |
| Auditoría y Validación Manual — **acciones** | **TO_BE · CONFIRMADA (ausencia)** | — | — | Todo botón de acción está deshabilitado permanentemente | Ninguno — `title="Disponible cuando se active Centro de Correcciones"` | STATIC_CODE `components/audit/FutureActionButton.tsx:1-20`, usado en las 5 secciones de tabla del audit shell |

## C. Administración — CRUD server-to-server (sin UI que los invoque)

| Funcionalidad | Estado | Actor | Entrada | Acción | Resultado | Evidencia técnica |
|---|---|---|---|---|---|---|
| CRUD `audit.data_quality_events` | AS_IS · CONFIRMADA (LOCAL_RUNTIME: `GET` con token → 200, 1 fila con `pageSize=1`) | Caller server-to-server con `x-nexus-admin-token` | JSON body (POST/PATCH) | Alta/edición/marcar resuelto/borrado | Registro de incidentes de calidad de dato | STATIC_CODE `app/api/admin/audit/data-quality-events/**`; **ninguna UI del repo lo invoca** (grep confirmado, ver `01`§frontend original) |
| CRUD `audit.pipeline_runs` | AS_IS · CONFIRMADA (LOCAL_RUNTIME: 200, 0 filas) | Caller server-to-server | JSON body | Registrar corrida de pipeline | Tabla vacía en runtime — **ningún script de `src/` la usa** | STATIC_CODE `app/api/admin/audit/pipeline-runs/**`; AUSENCIA CONFIRMADA de writer en `src/` |
| CRUD `manual_review.part_aliases` | AS_IS · CONFIRMADA (LOCAL_RUNTIME: 200, 0 filas) | Caller server-to-server | JSON body | Alta/edición/**baja lógica** (`active=false`, nunca hard-delete) | Alias manuales para la cascada de resolución de repuestos | STATIC_CODE `app/api/admin/manual-review/part-aliases/**` |
| CRUD `manual_review.ticket_link_overrides` | AS_IS · CONFIRMADA (LOCAL_RUNTIME: 200, 0 filas) | Caller server-to-server | JSON body | Alta/edición/**baja física** (hard-delete, sin columna `active`) | Correcciones manuales de vínculo ticket↔task | STATIC_CODE `app/api/admin/manual-review/ticket-link-overrides/**` |
| CRUD `stock.stock_movements` | AS_IS · CONFIRMADA (LOCAL_RUNTIME: 200, 0 filas) | Caller server-to-server | JSON body | Alta (`PENDING`) → `CONFIRMED`/`REVERSED`; borrado solo si `PENDING` (409 si no) | Registro manual de movimiento de stock — **no automático** | STATIC_CODE `app/api/admin/stock/movements/**`, guardia de estado en `[id]/route.ts:56-85` |

## D. Prometido/propuesto — sin implementación verificable en este commit

| Funcionalidad | Estado | Evidencia técnica |
|---|---|---|
| Automatización de descuento de stock en Dolibarr | TO_BE · PARCIALMENTE CONFIRMADA (scaffolding AS_IS, función pendiente de decisión) | `sql/060_stock.sql:1-5` ("forward-looking... el pipeline actual NO descuenta inventario"); `docs/PHASE_2_BACKLOG.md:27-31` |
| CERBERUS — motor formal de calidad de reportes (score `integridad_reporte`, clasificación apto/parcial/dudoso/no apto) | **LEGACY en código actual · COMMITTED como compromiso contractual** | STATIC_DOC + EXTERNAL_OFFICIAL_SOURCE `docs/legacy/relevamiento_maestro_nexus_cerberus.html:212-220,650-727` ("Monto cerrado Fase 1: $3.000.000 CLP", "Alcance: TODO NEXUS + TODO CERBERUS"); AUSENCIA CONFIRMADA en código · comando `git grep -i cerberus 4a3d055 -- . ':!docs/legacy'` → 0 resultados. Detalle en `00-product-north-star.md` y `11-product-decision-register.md` |
| JANUS — uptime/downtime con confianza formal | **LEGACY en código actual · COMMITTED como Fase 2 proyectada** | Mismo doc, `:220,273,518`; AUSENCIA CONFIRMADA de "JANUS" en código (mismo comando) |
| ATLAS — base de datos operacional unificada | **LEGACY en código actual · COMMITTED como Fase 2 proyectada** | Mismo doc, `:220,273,523`; AUSENCIA CONFIRMADA de "ATLAS" en código |
| Dashboard Equipment Lifecycle | **LEGACY (existe completo en otra branch) · TO_BE para esta branch** | `docs/TECH_DEBT_UNREPRODUCIBLE_TABLES.md:20-26`; tablas `marts.equipment_part_lifecycle_*`/`gold.equipment_part_lifecycle_*` existen en Postgres (snapshot) pero sin builder ni pantalla en `supabase-migration`; código completo vive en `cloud-d1-readonly`/`cloud-smoke-test` (GIT_HISTORY, no auditado como parte de este baseline) |

## Fuentes

`package.json` (raíz), `docs/ARCHITECTURE.md`, `docs/DATA_PIPELINE.md`, los 33 `route.ts` bajo `apps/nexus-bi-app/app/api/**`, `src/{miners,normalizers,resolvers,marts,gold,qa,db}/*.js`, `sql/000-060`, `docs/TECH_DEBT_UNREPRODUCIBLE_TABLES.md`, `docs/legacy/relevamiento_maestro_nexus_cerberus.html`, `docs/PHASE_2_BACKLOG.md`, sondeo LOCAL_RUNTIME de esta sesión (ver `00-audit-baseline.md`).
