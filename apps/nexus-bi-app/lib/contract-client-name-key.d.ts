// Declaración de tipos para contract-client-name-key.js -esta app tiene
// allowJs:false (no puede compilar/tipar el .js directamente). La lógica
// real vive únicamente en el .js hermano.
export function buildContractClientNameKey(value: unknown): string;
